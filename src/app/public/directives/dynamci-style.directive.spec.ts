import { DynamciStyleDirective } from './dynamci-style.directive';

describe('DynamciStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamciStyleDirective();
    expect(directive).toBeTruthy();
  });
});
