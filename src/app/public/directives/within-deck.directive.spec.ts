import { WithinDeckDirective } from './within-deck.directive';

describe('WithinDeckDirective', () => {
  it('should create an instance', () => {
    const directive = new WithinDeckDirective();
    expect(directive).toBeTruthy();
  });
});
