import { AvalibleCardPipe } from './avalible-card.pipe';

describe('AvalibleCardPipe', () => {
  it('create an instance', () => {
    const pipe = new AvalibleCardPipe();
    expect(pipe).toBeTruthy();
  });
});
