import { CardColorPipe } from './card-color.pipe';

describe('CardColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CardColorPipe();
    expect(pipe).toBeTruthy();
  });
});
