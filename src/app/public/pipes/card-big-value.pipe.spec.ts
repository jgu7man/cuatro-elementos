import { CardBigValuePipe } from './card-big-value.pipe';

describe('CardBigValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CardBigValuePipe();
    expect(pipe).toBeTruthy();
  });
});
